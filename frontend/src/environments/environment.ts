export const environment = {
    production: false,
    // URL du backend Spring Boot en local (accessible depuis le même réseau Docker ou via le port exposé)
    apiUrl: 'http://localhost:8080/api'
};
